type Query {
    shows(titleFilter: String): [Show]
}

type Show {
    title: String
    releaseYear: Int
}

type Mutation {
    addShow(input: ShowInput):Show
}

input ShowInput {
    title: String
    releaseYear: Int
}


